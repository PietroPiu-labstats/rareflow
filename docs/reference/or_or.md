# Default Operator %\|\|%

Returns `b` if `a` is NULL, otherwise returns `a`.

## Usage

``` r
a %||% b
```

## Arguments

- a:

  Primary value.

- b:

  Default value.

## Value

Either `a` or `b`.
